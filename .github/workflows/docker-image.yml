name: Docker Image CI

on:
  push:
    branches: [ "chargepoint/2023.02.x" ]
  pull_request:
    branches: [ "chargepoint/2023.02.x" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
